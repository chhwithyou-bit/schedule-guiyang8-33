--- v5-svelte-migration/src/components/ui/ThemeSwitcher.svelte
+++ v5-svelte-migration/src/components/ui/ThemeSwitcher.svelte
@@ -156,6 +156,7 @@
       scale: 0,
       display: 'block' // Show it
     });
+    inkWashNode.style.display = 'block';

     // Ink Wash expansion
     // To cover radius with a 100px circle (radius 50px), we scale by (radius * 2) / 100 = radius / 50
